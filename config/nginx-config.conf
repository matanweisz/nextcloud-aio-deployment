    # Change this IP to match your VM
    server 10.40.0.100:11000;

    # Connection pooling for better performance
    keepalive 32;
}

# WebSocket connection upgrade map
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

# HTTP to HTTPS redirect
server {
    listen 80;
    listen [::]:80;
    server_name nextcloud.infinitylabs.co.il;

    # Redirect all HTTP to HTTPS
    return 301 https://$server_name$request_uri;
}

# Main HTTPS server block
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name nextcloud.infinitylabs.co.il;

    # ========================================================================
    # SSL Configuration (Your Existing Certificates)
    # ========================================================================
    # IMPORTANT: Use your existing certificate paths
    ssl_certificate /etc/ssl/certs/nextcloud.infinitylabs.co.il.crt;
    ssl_certificate_key /etc/ssl/private/nextcloud.infinitylabs.co.il.key;

    # Modern SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;

    # SSL session caching
    ssl_session_cache shared:SSL:50m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    # OCSP stapling (if your cert supports it)
    # ssl_stapling on;
    # ssl_stapling_verify on;
    # ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;

    # ========================================================================
    # Security Headers
    # ========================================================================
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # ========================================================================
    # Upload and Timeout Configuration
    # ========================================================================
    # Maximum upload size (16GB)
    client_max_body_size 16G;

    # Buffer size for client body (important for large uploads)
    client_body_buffer_size 512k;

    # Timeouts (important for large file uploads and office documents)
    client_body_timeout 300s;
    client_header_timeout 300s;
    send_timeout 300s;

    # Proxy timeouts
    proxy_connect_timeout 300s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;

    # ========================================================================
    # Buffering Configuration
    # ========================================================================
    # Disable buffering for large uploads (better for Nextcloud)
    proxy_buffering off;
    proxy_request_buffering off;
    proxy_max_temp_file_size 0;

    # ========================================================================
    # Logging
    # ========================================================================
    access_log /var/log/nginx/nextcloud.access.log combined;
    error_log /var/log/nginx/nextcloud.error.log warn;

    # ========================================================================
    # Main Proxy Location
    # ========================================================================
    location / {
        # Proxy pass to Nextcloud AIO backend
        proxy_pass http://nextcloud-aio-backend;

        # Essential headers for Nextcloud
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # WebSocket support (required for Nextcloud Talk and Notify Push)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        # Don't modify redirects
        proxy_redirect off;

        # Connection pooling
        proxy_set_header Connection "keep-alive";
    }

    # ========================================================================
    # Specific Locations for Nextcloud Features
    # ========================================================================

    # Notify Push endpoint (for real-time notifications)
    location /push/ {
        proxy_pass http://nextcloud-aio-backend/push/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Longer timeout for push connections
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # Collabora (Nextcloud Office) endpoint
    location /browser {
        proxy_pass http://nextcloud-aio-backend/browser;
        proxy_set_header Host $host;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Longer timeout for office documents
        proxy_read_timeout 3600s;
    }

    # Talk (video conferencing) signaling endpoint
    location /standalone-signaling {
        proxy_pass http://nextcloud-aio-backend/standalone-signaling;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Whiteboard endpoint
    location /whiteboard {
        proxy_pass http://nextcloud-aio-backend/whiteboard;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ========================================================================
    # Static Content Optimization
    # ========================================================================
